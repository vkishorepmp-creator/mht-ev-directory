# Supabase connection — copy to `.env` for local dev, set in Vercel for prod.
# Use the ROTATED anon key (the old one is in public git history). See OWNER_SETUP.md.
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your-rotated-anon-key
